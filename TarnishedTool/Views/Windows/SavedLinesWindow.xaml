<windows:TopmostWindow x:Class="TarnishedTool.Views.Windows.SavedLinesWindow"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:controls="clr-namespace:TarnishedTool.Controls"
                       xmlns:windows="clr-namespace:TarnishedTool.Views.Windows"
                       mc:Ignorable="d"
                       Style="{StaticResource DefaultWindowStyle}"
                       WindowStyle="None"
                       AllowsTransparency="True"
                       Title="Saved Lines" Height="420" Width="380">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:TitleBar Grid.Row="0" Title="Saved Lines" />

        <Grid Grid.Row="1">
            <CheckBox x:Name="AlwaysOnTopCheckBox"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Center"
                      Margin="0,0,10,0"
                      Checked="AlwaysOnTopCheckBox_Checked"
                      Unchecked="AlwaysOnTopCheckBox_Unchecked">
                Always on Top
            </CheckBox>
        </Grid>

        <ListBox Grid.Row="2"
                 Margin="10,0,10,0"
                 ItemsSource="{Binding Lines}"
                 SelectedItem="{Binding SelectedLine, Mode=TwoWay}"
                 MouseDoubleClick="SavedLinesList_MouseDoubleClick"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="4,3" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="1" Text="{Binding BestText}" Foreground="Gold"
                                   FontWeight="SemiBold" Margin="10,0,0,0" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Load" Width="70" Height="28" Margin="0,0,6,0"
                    Command="{Binding LoadCommand}" />
            <Button Content="Save current" Width="95" Height="28" Margin="0,0,6,0"
                    Command="{Binding SaveCurrentCommand}" />
            <Button Content="Rename" Width="70" Height="28" Margin="0,0,6,0"
                    Command="{Binding RenameCommand}" />
            <Button Content="Delete" Width="70" Height="28"
                    Command="{Binding DeleteCommand}" />
        </StackPanel>
    </Grid>
</windows:TopmostWindow>
